import { LoginRequestDto } from "../../../dto";
import { BaseDialogProps } from "../../base/base-dialog";

export function ProfileDialogFooter(_props: BaseDialogProps<LoginRequestDto>) {
  // #region State ------------------------------------------------------------
  // #endregion

  // #region Rendering --------------------------------------------------------
  return (
    <div>Profile view footer</div>
  );
  // #endregion
}
