// FEATURE extended progress reporting with two progress bars and eventually percentage
export type ProgressCallbackValue = string;
export type ProgressCallback = (value: ProgressCallbackValue) => void;
